//
// Created by Eddie Hoyle on 8/05/17.
//

#ifndef STREAKER_PADDING_HH
#define STREAKER_PADDING_HH

enum PaddingType {
    kHash,
    kNuke
};

class Padding {

    /*
     * Represents padding formatter
     * - input number to get frame number?
     * - is this just a frame string formatter?
     */

public:
    Padding();
    ~Padding();

};

#endif //STREAKER_PADDING_HH
